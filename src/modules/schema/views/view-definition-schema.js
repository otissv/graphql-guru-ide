import React from 'react';
import Definition from '../components/Definition-schema';
import SchemaContainer from '../containers/container-schema';

export default props => <SchemaContainer {...props} component={Definition} />;
