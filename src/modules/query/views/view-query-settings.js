import React from 'react';
import Settings from '../components/Settings-query';
import SettingsContainer from '../containers/settings-container-query';

const SettingsView = () => <SettingsContainer component={Settings} />;

export default SettingsView;
