import React from 'react';
import Home from '../components/Home-app';
import SharedContainer from '../../../modules/shared/containers/container-shared';

export default (props) => <SharedContainer {...props} component={Home} />;

