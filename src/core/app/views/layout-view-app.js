import React from 'react';
import Layout from '../components/Layout-app';
import LayoutContainer from '../containers/layout-container-app';

export default (props) => <LayoutContainer {...props} component={Layout} />;

